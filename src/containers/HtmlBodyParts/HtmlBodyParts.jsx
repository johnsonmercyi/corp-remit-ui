export const Header = (props) => (
    <header>
        {props.children}
    </header>
);

export const Main = (props) => (
    <main>
        {props.children}
    </main>
);

export const Footer = (props) => (
    <footer>
        {props.children}
    </footer>
);